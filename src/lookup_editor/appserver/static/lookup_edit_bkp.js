__non_webpack_require__([
         "jquery",
         "underscore",
         "backbone",
         "splunkjs/mvc/simplexml/ready!"
     ], function(
         $,
         _,
         Backbone,
     )
     {
        require(['lookup-static/js/views/LookupEditView'], function(LookupEditView){
    /**
     * Everything is being managed from xml & LookupEditView
    */  
    //      var lookupEditView = new LookupEditView({
    //     	 el: $('#lookups_editor')
    //      });
         
    //      lookupEditView.render();
     })
    }
);